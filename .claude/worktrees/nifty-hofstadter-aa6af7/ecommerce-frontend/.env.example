# {host} is replaced with the browser hostname so storefront requests reach
# the backend on the tenant's own hostname. See src/config/env.ts.
VITE_API_URL=http://{host}:4000/api/v1
VITE_STORE_DOMAIN=platform.localhost
